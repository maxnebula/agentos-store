# AgentOS Store - Agent Safeguards

## CRITICAL
- NEVER use db uninstall, pm clear, or wipe app data
- Use db install -r to upgrade (preserves data)
- Before bumping Room DB version, use manual migration, not destructive fallback
- Ask user before any destructive operation

